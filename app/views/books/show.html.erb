<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @book.name %>
</p>
<p>
  <strong>Ages:</strong>
  <%= @book.ages.map {|a| a.name}.join(', ') %>
</p>
<p>
  <strong>Category:</strong>
  <%= @book.category.name if @book.category%>
</p>

<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', books_path %>
