<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @age.name %>
</p>

<p>
  <strong>Book Names:</strong>
  <%= @age.books.map {|b| b.name}.join(', ') %>
</p>

<%= link_to 'Edit', edit_age_path(@age) %> |
<%= link_to 'Back', ages_path %>
